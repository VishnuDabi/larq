import { createSlice } from "@reduxjs/toolkit";
const initialState=[{}]
export const BucketSlice = createSlice({
    name: 'bucket', initialState,
    reducers: {
        addProduct: (state) => {
            
        }
    }
})